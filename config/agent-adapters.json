{
  "version": 1,
  "adapters": {
    "codex": {"argv": ["codex", "exec", "--sandbox", "workspace-write", "--ephemeral", "-"], "prompt_transport": "stdin"},
    "codex-reviewed": {"argv": ["codex", "exec", "--approve-for-me", "-"], "prompt_transport": "stdin"}
  },
  "timeout_seconds": 3600
}
