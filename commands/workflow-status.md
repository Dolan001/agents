---
description: Show completed phases, current checkpoint, blockers, and next action
argument-hint: ""
---
<!-- managed-by: ai_workflow -->
# Workflow status

Read `{{WORKFLOW_PATH}}/commands/references/start-command-contract.md`, then run:

```text
./{{WORKFLOW_PATH}}/bin/ai status --project . --json
```

Summarize evidence and the exact next command. Do not change project state.
