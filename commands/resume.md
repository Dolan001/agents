# `ai resume`

Validate the saved checkpoint and target Git baseline, release expired leases, and
continue from the first incomplete blueprint node. Never rerun a verified node
unless one of its declared inputs changed.
