# Requirements scope

Requirements analysis may write generated documentation, dependency graphs, task
contracts, and evidence. It must not implement requirements. Every requirement and
task must carry a stable requirement ID.
