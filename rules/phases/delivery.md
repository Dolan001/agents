# Delivery scope

Aggregate evidence without hiding failures. Report incomplete requirements and
remaining gaps. Commits must describe requirement IDs. Remote push remains disabled
until the user explicitly executes it on a safe branch.
