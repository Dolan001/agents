# Integration scope

Use OpenAPI as the client/server contract. Replace fixture adapters deliberately,
configure authentication transport, CORS and CSRF as applicable, and cover
validation, authorization, expired session, network, and server failure paths.
