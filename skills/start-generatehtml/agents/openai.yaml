interface:
  display_name: "start-generatehtml"
  short_description: "Run the start-generatehtml workflow stage"
  default_prompt: "Use $start-generatehtml to run the requested workflow stage from the current project."
