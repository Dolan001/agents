interface:
  display_name: "start-backend"
  short_description: "Run the start-backend workflow stage"
  default_prompt: "Use $start-backend to run the requested workflow stage from the current project."
