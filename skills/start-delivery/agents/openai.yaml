interface:
  display_name: "start-delivery"
  short_description: "Run the start-delivery workflow stage"
  default_prompt: "Use $start-delivery to run the requested workflow stage from the current project."
