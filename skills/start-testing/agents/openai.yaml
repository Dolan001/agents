interface:
  display_name: "start-testing"
  short_description: "Run the start-testing workflow stage"
  default_prompt: "Use $start-testing to run the requested workflow stage from the current project."
