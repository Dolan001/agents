interface:
  display_name: "Workflow status"
  short_description: "Inspect workflow progress, issues, and next safe action"
  default_prompt: "Use $workflow-status to report completed phases, tracked build issues, blockers, and the exact next safe action."
