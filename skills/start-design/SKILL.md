---
name: start-design
description: Create only the design specification from the required PRD and optional HTML or screenshots, stopping before HTML generation. Use when the user invokes start-design or asks to work only on design planning.
---

# Start design specification

Read `.agents/commands/references/start-command-contract.md`, then invoke
`./.agents/bin/ai start-design` with `--adapter codex`. Stop after
`HTML/design-specification.md`; do not continue to HTML or application code.
